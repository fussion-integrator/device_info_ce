name: device_info_ce_example
description: Example app for device_info_ce plugin.
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  device_info_ce:
    path: ../
  cupertino_icons: ^1.0.2
  fl_chart: ^0.68.0
  animate_do: ^3.3.4
  glassmorphism: ^3.0.0
  shimmer: ^3.0.0
  share_plus: ^7.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true